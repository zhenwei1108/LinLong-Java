package com.github.zhenwei.sdk.util;

import java.util.Arrays;

public class ByteArrayUtil {

    public void mergeBytes(byte[]... data) {
        if (data != null && data.length > 0) {
            Arrays.stream(data).forEach(bytes->{

            });


        }

    }


}
